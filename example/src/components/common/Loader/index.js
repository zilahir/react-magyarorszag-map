import React from 'react'
import BarLoader from 'react-spinners/BarLoader'

/**
* @author zilahir
* @function Loader
**/

const Loader = (props) => {
  return(
    <div>
      <BarLoader
      />
    </div>
   )

 }

export default Loader